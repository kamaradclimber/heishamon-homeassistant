{
  "config": {
    "abort": {
      "instance_already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    },
    "step": {
      "confirm": {
        "description": "All configured!"
      },
      "user": {
        "title": "Configure HeishaMon manually",
        "description": "Enter the MQTT topic prefix configured on your HeishaMon device.",
        "data": {
          "discovery_prefix": "MQTT topic prefix"
        },
        "data_description": {
          "discovery_prefix": "The MQTT topic prefix used by your HeishaMon (e.g. 'panasonic_heat_pump/'). Must match the prefix configured in the HeishaMon web interface."
        }
      }
    }
  }
}
